<?xml version="1.0" encoding="utf-8" ?>
<ContentView
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    x:Class="IntelligentHabitacion.App.Template.Button.DeleteButton">
    <ContentView.Content>
        <Grid ColumnSpacing="30" x:Name="GridCard">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"></ColumnDefinition>
                <ColumnDefinition Width="*"></ColumnDefinition>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="40"></RowDefinition>
            </Grid.RowDefinitions>
            <StackLayout Grid.Column="0" Grid.Row="0" BackgroundColor="Transparent">
                <StackLayout.GestureRecognizers>
                    <TapGestureRecognizer Tapped="Button_OnTapped"></TapGestureRecognizer>
                </StackLayout.GestureRecognizers>
                <Label Style="{x:StaticResource LabelLight}" x:Name="LabelTitle" HorizontalOptions="StartAndExpand" HorizontalTextAlignment="Start" FontSize="12" VerticalOptions="CenterAndExpand"></Label>
            </StackLayout>
        </Grid>
    </ContentView.Content>
</ContentView>